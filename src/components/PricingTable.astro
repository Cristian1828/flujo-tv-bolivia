---
const plans = [
  { 
    duration: "1 MES", 
    price: "9", 
    perMonth: "9",
    badge: "ZONA PLAY",
    color: "from-blue-500 to-cyan-500",
    icon: "⚽"
  },
  { 
    duration: "3 MESES", 
    price: "27", 
    perMonth: "9",
    badge: "⭐ CLUB VIP",
    color: "from-green-500 to-emerald-500",
    icon: "🏆",
    popular: true
  },
  { 
    duration: "6 MESES", 
    price: "49", 
    perMonth: "8.17",
    badge: "PALCO PREMIUM",
    color: "from-orange-500 to-red-500",
    icon: "👑",
    popular: false
  },
  { 
    duration: "12 MESES", 
    price: "95", 
    perMonth: "7.92",
    badge: "TEMPORADA COMPLETA",
    color: "from-purple-500 to-pink-500",
    icon: "⚡"
  }
];
---

<section id="planes" class="relative py-16 md:py-32 bg-gradient-to-b from-orange-50 via-yellow-50 to-red-50">
  {/* Decorative elements */}
  <div class="absolute inset-0 overflow-hidden opacity-10">
    <div class="absolute top-20 left-20 w-64 h-64 bg-orange-400 rounded-full blur-3xl"></div>
    <div class="absolute bottom-20 right-20 w-96 h-96 bg-red-400 rounded-full blur-3xl"></div>
  </div>

  <div class="relative z-10 max-w-7xl mx-auto px-4 md:px-6">
    <!-- Header -->
    <div class="text-center mb-12 md:mb-20">
      <div class="inline-block mb-6 md:mb-8">
        <div class="flex items-center gap-2 md:gap-3 px-4 md:px-8 py-3 md:py-4 bg-white rounded-full shadow-xl">
          <span class="text-2xl md:text-4xl">💰</span>
          <span class="font-black text-lg md:text-2xl text-gray-900">Planes & Precios</span>
        </div>
      </div>
      
      <h2 class="text-4xl md:text-6xl lg:text-8xl font-black text-gray-900 mb-4 md:mb-6 leading-tight px-4">
        Elige tu plan
        <br />
        <span class="text-transparent bg-clip-text bg-gradient-to-r from-red-600 via-orange-600 to-yellow-600">
          perfecto
        </span>
      </h2>
      
      <p class="text-lg md:text-2xl text-gray-700 max-w-2xl mx-auto px-4">
        Todos con acceso completo. Sin límites. Sin sorpresas.
      </p>
    </div>

    <!-- Cards horizontales storytelling -->}
    <div class="space-y-4 md:space-y-6 max-w-5xl mx-auto mb-12 md:mb-16">
      {plans.map((plan, index) => (
        <div 
          class={`relative group ${plan.popular ? 'md:scale-105' : ''}`}
          style={`animation-delay: ${index * 0.1}s;`}
        >
          {plan.popular && (
            <div class="absolute -top-3 md:-top-4 left-1/2 transform -translate-x-1/2 z-10">
              <div class="bg-gradient-to-r from-orange-500 to-red-500 text-white px-4 md:px-8 py-1.5 md:py-2 rounded-full font-black text-xs md:text-sm shadow-xl">
                ⭐ MÁS POPULAR
              </div>
            </div>
          )}

          <div class={`bg-white rounded-2xl md:rounded-3xl shadow-2xl overflow-hidden group-hover:shadow-3xl group-hover:scale-[1.02] transition-all duration-500 border-4 ${plan.popular ? 'border-orange-500' : 'border-transparent'}`}>
            <div class="grid md:grid-cols-3 gap-0">
              {/* Left - Icon & Duration */}
              <div class={`bg-gradient-to-br ${plan.color} p-6 md:p-8 flex flex-col items-center justify-center text-white group-hover:scale-105 transition-transform duration-500`}>
                <div class="text-6xl md:text-8xl mb-3 md:mb-4 group-hover:rotate-12 transition-transform duration-500">{plan.icon}</div>
                <div class="text-xl md:text-3xl font-black text-center">{plan.duration}</div>
                {plan.badge && (
                  <div class="mt-2 md:mt-3 px-3 md:px-4 py-1 bg-white/20 backdrop-blur rounded-full text-xs md:text-sm font-bold">
                    {plan.badge}
                  </div>
                )}
              </div>

              {/* Middle - Price */}
              <div class="p-6 md:p-8 flex flex-col justify-center border-r border-gray-100">
                <div class="text-xs md:text-sm text-gray-500 mb-2">Precio Total</div>
                <div class="flex items-start mb-2 md:mb-3">
                  <span class="text-xl md:text-2xl font-bold text-gray-900 mr-1">$</span>
                  <span class="text-4xl md:text-6xl font-black text-gray-900 group-hover:text-orange-600 transition-colors duration-300">{plan.price}</span>
                </div>
                <div class="text-gray-600">
                  <span class="font-bold text-base md:text-xl">${plan.perMonth}</span> <span class="text-sm md:text-base">USD/mes</span>
                </div>
              </div>

              {/* Right - Features & CTA */}
              <div class="p-6 md:p-8 flex flex-col justify-center">
                <ul class="space-y-2 mb-4 md:mb-6">
                  <li class="flex items-center gap-2 text-sm md:text-base text-gray-700">
                    <span class="text-green-500 text-lg md:text-xl flex-shrink-0">⚽</span>
                    <span>El Estadio en tu Sala</span>
                  </li>
                  <li class="flex items-center gap-2 text-sm md:text-base text-gray-700">
                    <span class="text-green-500 text-lg md:text-xl flex-shrink-0">⚡</span>
                    <span>Sin cortes, sin excusas</span>
                  </li>
                  <li class="flex items-center gap-2 text-sm md:text-base text-gray-700">
                    <span class="text-green-500 text-lg md:text-xl flex-shrink-0">🏆</span>
                    <span>Acción global en vivo</span>
                  </li>
                </ul>
                
                <a 
                  href={`https://wa.me/51921445800?text=¡Quiero%20el%20plan%20${plan.duration}%20por%20$${plan.price}%20USD!%20⚽`}
                  target="_blank"
                  class={`block w-full py-3 md:py-4 text-center bg-gradient-to-r ${plan.color} text-white font-black text-base md:text-lg rounded-xl md:rounded-2xl hover:shadow-xl hover:scale-105 transition-all duration-300`}
                >
                  ELEGIR PLAN
                </a>
              </div>
            </div>
          </div>
        </div>
      ))}
    </div>


  </div>
</section>

<style>
  [style*="animation-delay"] {
    animation: slideInRight 0.8s ease-out backwards;
  }

  @keyframes slideInRight {
    from {
      opacity: 0;
      transform: translateX(50px);
    }
  }
  
  @media (max-width: 768px) {
    @keyframes slideInRight {
      from {
        opacity: 0;
        transform: translateY(20px);
      }
    }
  }
</style>
